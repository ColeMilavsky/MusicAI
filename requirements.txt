# Core scientific and plotting stack
numpy>=1.23.0
pandas
matplotlib
Pillow
pathlib
glob2

# Audio processing
librosa>=0.10.0
soundfile

# Machine learning
scikit-learn

# Deep learning (TensorFlow + Keras)
tensorflow==2.13.0
keras==2.13.1

# Optional (used internally by librosa/soundfile)
numba
audioread
# Install via: conda install -c conda-forge ffmpeg
# OR (if using pip): pip install imageio[ffmpeg]
imageio[ffmpeg]